import 'population.dart';

class EliteStrategy {
  int eliteStrategyProbability;
  var maximization;

  EliteStrategy(this.eliteStrategyProbability, this.maximization);

  Population eliteStrategy(population) {
    // TODO: implement inversion
    return population;
  }
}
